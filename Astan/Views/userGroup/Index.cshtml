@model IEnumerable<Astan.Models.userGroup>

@{
    ViewBag.Title = "";
}


<p>
    @Html.ActionLink("جدید", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.userGroupName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.userGroupName)
        </td>
        <td>
            @Html.ActionLink("ویرایش", "Edit", new { id=item.userGroupID }) |
            @Html.ActionLink("مشاهده", "Details", new { id=item.userGroupID }) |
            @Html.ActionLink("حذف", "Delete", new { id=item.userGroupID })
        </td>
    </tr>
}

</table>
